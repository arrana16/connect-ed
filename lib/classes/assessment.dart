class Assessment {
  String className;
  DateTime date;
  String title;
  String description;

  Assessment(
      {required this.className,
      required this.date,
      required this.title,
      required this.description});
}
